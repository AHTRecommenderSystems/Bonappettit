.container#createDish
  .col-xs-8.col-xs-offset-2
    .row
      h4 Describe las características del platillo
      form.form
        .col-xs-12
          input.form-control(type="text" placeholder="Nombre del platillo" required="" ng-model="createDish.dish.name")
        .col-xs-12
          textarea.form-control.margin_form_input(rows="5" placeholder="Descripción" required="" ng-model="createDish.dish.description")
        .col-xs-12
          .button.btn.btn-default.margin_form_input_3(ngf-select='', ng-model='createDish.picture', name='file', ngf-pattern="'image/*'", ngf-accept="'image/*'", ngf-max-size='20MB', ngf-min-height='100', ngf-resize='{width: 635, height: 400}') Una imagen dice más que mil palabras. Agrega una imagen del platillo.
          button.btn.btn-default(ng-if="createDish.picture" ng-click="createDish.deleteFile()") Borrar selección
          img(ngf-thumbnail="createDish.picture || ''")
        .col-xs-12
          h4 Selecciona todas las categorías a las que pertenece el platillo
          img(ng-if="!createDish.characteristics" src="assets/images/loading.gif")
          uib-accordion(close-others='oneAtATime')
            uib-accordion-group(is-open='status.open' ng-repeat="category in createDish.characteristics")
              uib-accordion-heading
                | {{category.key}}
                i.pull-right.glyphicon(ng-class="{'glyphicon-chevron-down': status.open, 'glyphicon-chevron-right': !status.open}")
              .characteristic.col-md-3.col-sm-6.pull-left(ng-repeat="characteristic in category.values")
                .row
                  input(type='checkbox', name="characteristic.name" ng-model="createDish.dishCharacteristics[characteristic.id]" id="{{characteristic.id}}")
                  label(for="{{characteristic.id}}" style="display: block; margin: auto; background: url(assets/images/characteristics/{{characteristic.picture}}); background-size: 42px; background-repeat: no-repeat;" id="{{'label' + characteristic.id}}" ng-click="createDish.selectCharacteristic(characteristic)")
                .row.text-center
                  | {{characteristic.name}}
        .col-xs-12
          input.btn.btn-success.button_create.save_dish(type="submit" value="Guardar platillo" ng-click="createDish.saveDish()" style="width: 100%;")

  uib-alert(ng-repeat="alert in createDish.alerts" type="{{alert.type}}" close="createDish.closeAlert($index)") {{alert.msg}}